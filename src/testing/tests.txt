int64 x = 0;
float32 y;


structPoint {
  float32 x;
  float64 y;
}

enum Color {
  RED,
  GREEN,
  BLUE,
  DARK_GREEN
}

int64 main(string[] args) {
  print(Point(2, 5.5));
  print("hello);
  return 0 * 3 + 2 ** 3 - 1;
  x = 3 ** 2; // assign
  print(y); // call
}

try {
  throw new Error("Something went wrong");
} catch (Error e) {
  print(e);
} finally {
  print("Finally");
}


uint64 gcd(uint64 a, uint64 b) {
  if (b == 0) {
    return a;
  }
  return gcd(b, a % b);
}

// ===========================================
// Write down all operators and precedence/assoc

Binary
------
&& and || same prec L
|| L
<= < == != >= > NON
+ - L
* / % L
** (right associative) R

-2 + 5

5 +- 2

x <- y

----------2

-2**2

2**-2